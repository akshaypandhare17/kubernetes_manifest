apiVersion: v1
kind: Service
metadata:
  labels:
    app: demo-website
  name: demo-website
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: demo-website
  type: NodePort
